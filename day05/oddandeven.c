#include<stdio.h>
int main()
{
    int n=0,i=0,a[0],s=0;
    scanf("%d",&n);
    int a[n];
    for(int i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
        if(n==0);       
    }
    if(a[i]%2==0)
        es=s+a[i];
    else
        os=s+a[i];

    return 0;
} 
